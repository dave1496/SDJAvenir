// Style.js

const view = {
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 5,
    marginBottom: 5,
    borderRadius: 2,
    padding: 15,
    backgroundColor: '#FFFFFF',
};

const text = {
    color: '#000000',
    fontSize: 24,
};

const buttonText = {
    color: '#FFFFFF',
};

const button = {
    marginTop: 25,
    marginBottom: 25,
};

export default { view, text, buttonText, button };