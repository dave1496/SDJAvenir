// Style.js

const form = {
    marginTop: 50,
    flexDirection: 'column',
    alignItems: 'center',
};

const item = {
    display: 'flex',
    flexDirection: 'row',
    marginBottom: 15,
    backgroundColor: '#FFF',
    borderRadius: 5,
    borderWidth: 1,
    borderColor: '#000',
};

const icon = {
    color: '#000',
    marginLeft: 10,
};

const button = {
    marginTop: 20,
};

export default { form, item, icon, button };