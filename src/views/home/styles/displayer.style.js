// Style.js

const grid = {
  flexDirection: 'row',
  alignItems: 'center',
  marginTop: 5,
  marginBottom: 5,
  borderRadius: 2,
  paddingLeft: 10,
  backgroundColor: '#FFFFFF',
};

const text = {
  fontSize: 18,
};

const leftCol = {
  paddingTop: 15,
  paddingBottom: 15,
};

const button = {
  height: '100%',
}

export default { grid, text, leftCol, button };