// Style.js

const text = {
  fontSize: 20,
  paddingTop: 5
};

const paypalText = {
  fontSize: 20,
  paddingTop: 5,
  color: '#FFFFFF',
}

const button = {
  flexDirection: 'row',
  alignItems: 'center',
};

export default { text, paypalText, button };